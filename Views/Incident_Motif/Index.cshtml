@model IEnumerable<Test13.Models.Incident_Motif>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Motif)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Motif_Symbole)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Motif)
        </td>
        <td>
            <img src="@Html.DisplayFor(modelItem => item.Motif_Symbole))" width="50px" height="50px" />
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Incident_MotifID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Incident_MotifID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Incident_MotifID })
        </td>
    </tr>
}

</table>
